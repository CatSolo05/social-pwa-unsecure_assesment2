# social-pwa-unsecure

Set `SECRET_KEY` in the environment before starting the app.
Optional: set `VAPID_PUBLIC_KEY` to supply the public push key from Flask instead of the default server-side fallback.

Make sure to run the application on port 5000 as that is the port flask is configured to 